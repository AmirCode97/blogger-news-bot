# Blogger News Bot 🤖

ربات هوشمند و تمام‌خودکار برای دریافت، پردازش و انتشار اخبار حقوق بشر و سیاسی ایران در بلاگر.

## ✨ ویژگی‌ها (Features)

این ربات به صورت خوکار اخبار را از منابع معتبر دریافت کرده و پس از پردازش، با ظاهری استاندارد در وبلاگ منتشر می‌کند.

* **منابع خبری متنوع:**
  * ایران اینترنشنال (Iran International)
  * رادیو فردا (Radio Farda)
  * مجموعه فعالان حقوق بشر در ایران (HRA News)
  * کانون حقوق بشر ایران (IranHRS)
  * کانون دفاع از حقوق بشر در ایران (Bashariyat)
* **استخراج هوشمند:** دریافت متن کامل خبر (Full Text) و تصاویر با کیفیت اصلی، حتی از سایت‌هایی که فیلتر هستند.
* **هوش مصنوعی (Gemini AI):** استفاده از AI برای بهینه‌سازی تیترها، خلاصه‌سازی و ترجمه در صورت نیاز.
* **مدیریت هوشمند:**
  * **حذف تکراری‌ها:** سیستم هوشمند تشخیص اخبار تکراری.
  * **پروکسی چرخشی:** استفاده از لیست پروکسی‌های قدرتمند برای عبور از فیلترینگ و Scrape کردن سایت‌ها.
* **قالب اختصاصی:** هماهنگ با قالب طراحی شده `blogger_theme_final.xml` برای نمایش زیبای اخبار.

## 🚀 نحوه نصب و اجرا

### ۱. نصب پیش‌نیازها

```bash
pip install -r requirements.txt
```

### ۲. تنظیمات (Configuration)

فایل `.env` را ایجاد کنید و اطلاعات زیر را در آن وارد نمایید:

```env
BLOG_ID=your_blog_id
GEMINI_API_KEY=your_api_key
TELEGRAM_BOT_TOKEN=optional
TELEGRAM_CHAT_ID=optional
```

همچنین فایل `credentials.json` (مربوط به Google OAuth) را در پوشه اصلی قرار دهید.

### ۳. اجرا (Usage)

**اجرای دستی (یک‌بار):**
برای به‌روزرسانی فوری وبلاگ:

```bash
python main.py --once
```

**اجرای خودکار (حلقه):**
برای روشن گذاشتن ربات (بررسی هر ۵ ساعت):

```bash
python main.py
```

**پاک‌سازی وبلاگ (Cleanup):**
اگر پست تکراری در وبلاگ مشاهده کردید، این دستور را اجرا کنید تا به صورت خودکار نسخه قدیمی را حذف و نسخه جدید را نگه دارد:

```bash
python cleanup_blog.py
```

## ⚙️ اجرای خودکار با GitHub Actions

این پروژه برای اجرا روی GitHub Actions پیکربندی شده است (فایل `news-bot.yml`).
ربات به صورت پیش‌فرض **هر ۵ ساعت یک‌بار** اجرا می‌شود.

**Secrets مورد نیاز در گیت‌هاب:**
برای فعال‌سازی، مقادیر زیر را در بخش Settings > Secrets مخزن وارد کنید:

* `BLOG_ID`
* `GEMINI_API_KEY`
* `GOOGLE_CREDENTIALS_JSON` (محتوای فایل جیسون)
* `GOOGLE_TOKEN_PICKLE` (توکن احراز هویت Base64 شده)

---
**License**: MIT
Developed by AmirCode97
